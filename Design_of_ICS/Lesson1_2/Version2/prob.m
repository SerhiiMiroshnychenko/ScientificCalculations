%{
% ЗАГОТОВКА ДЛЯ ЕКСПЕРИМЕНТІВ З НЕЧІТКИМИ МНОЖИНАМИ
% ==================================================
% Призначення: Файл-шаблон для швидких експериментів та тестування
% операцій над нечіткими множинами
%
% Структура файлу:
%   - Закоментований простий варіант (рядки 14-15)
%   - Активний розширений варіант (рядки 17-24)
%
% Активний код створює:
%   - Детальну сітку з 501 точки
%   - Широкий діапазон від -20 до 20
%   - Дві гаусові функції A та B
%
% Параметри:
%   A: гаусова з σ=1, центр=5
%   B: гаусова з σ=1, центр=7
%
% Використання:
%   1. Розкоментуйте рядок plot(x,A,x,B) для візуалізації
%   2. Додайте операції: max(A,B), min(A,B), probor, prod
%   3. Експериментуйте з параметрами функцій
%
% Застосування: Швидке тестування, навчальні експерименти,
% прототипування нових комбінацій функцій належності
%}

% === ПРОСТИЙ ВАРІАНТ (закоментовано) ===
%x = (0:0.2:10);
%plot(x,A,x,B)

% === РОЗШИРЕНИЙ ВАРІАНТ (активний) ===
% Кількість точок для високої точності
N = 501;

% Ліва межа діапазону
minX = -20;

% Права межа діапазону
maxX = 20;

% Створення детальної сітки точок
x = linspace(minX,maxX,N);

% Перша гаусова функція: вузька (σ=1), центр у x=5
A = gaussmf(x, [1 5]);

% Друга гаусова функція: вузька (σ=1), центр у x=7
B = gaussmf(x, [1 7]);
